# this will be used for the functions to calculate the position of the objects

import variables as var

def height_percent(percent):
    return (var.height * percent) / 100

def width_percent(percent):
    return (var.width * percent) / 100